{
  "name": "ripple-recursive-bench",
  "private": true,
  "type": "module",
  "version": "0.0.2",
  "description": "recursive-context bench fixture for ripple — exposes window.__mount/__update/__unmount/__reset for the Playwright harness.",
  "scripts": {
    "dev": "vite --host --port 5184 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5184 --strictPort"
  },
  "dependencies": {
    "ripple": "workspace:*"
  },
  "devDependencies": {
    "@ripple-ts/vite-plugin": "workspace:*",
    "vite": "catalog:default"
  }
}
