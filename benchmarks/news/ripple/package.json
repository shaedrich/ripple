{
  "name": "ripple-news-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "news-site SSR+hydration bench fixture for the original Ripple framework (authored in .tsrx, compiled via @tsrx/ripple).",
  "scripts": {
    "dev": "vite --host --port 5194 --strictPort"
  },
  "dependencies": {
    "ripple": "workspace:*"
  },
  "devDependencies": {
    "@tsrx/ripple": "workspace:*",
    "vite": "catalog:default"
  }
}
